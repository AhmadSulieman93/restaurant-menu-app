# ✅ الحل السريع - إنشاء Tables في Supabase

## ⚡ الخطوات السريعة:

### 1. في Supabase Dashboard:

1. من القائمة الجانبية: اضغط على **SQL Editor**
2. اضغط **"New query"**
3. انسخ كل محتوى ملف: `backend/RestaurantMenu.API/Migrations/SQL_SCRIPT.sql`
4. الصقه في SQL Editor
5. اضغط **"Run"** (أو F5)

---

### 2. بعد إنشاء الـTables:

**شغّل Backend فقط** - سيتم Seed Data تلقائياً:

```bash
cd backend/RestaurantMenu.API
dotnet run
```

**✅ سيحدث تلقائياً:**
- ✅ إنشاء الحسابات (Admin + Owners)
- ✅ إنشاء المطاعم (2 مطاعم)
- ✅ إنشاء الأصناف (80+ صنف)

---

## 📋 SQL Script موجود في:

`backend/RestaurantMenu.API/Migrations/SQL_SCRIPT.sql`

**انسخ كل محتوى الملف والصقه في SQL Editor!**

---

## ✅ بعد ذلك:

1. شغّل Backend: `dotnet run`
2. شغّل Frontend: `npm run dev`
3. سجّل دخول: `admin@restaurantmenu.com` / `Admin@123`

---

🎉 **هذا أسرع من البحث عن Connection String!**

